Found this to be pretty tough. The key intuition was realizing that checking whether we should take or skip our current spell is determined by whether we want to disable `power[i] - 2`, `power[i] - 1` IF we do the processing in a sorted order.